export default function loadInfo(req) {
  return new Promise((resolve) => {
    //req.session.user
    // resolve({
    //   message: 'This came from the api server',
    //   time: Date.now()
    // });
  });
}
